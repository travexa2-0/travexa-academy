# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Video fuente del hero (no se commitea — binario pesado; frames van a public/)
assets/source/

# imágenes de referencia locales (no versionar)
_ref/

# docs de negocio/técnico viven en la carpeta padre (~/Projects/travexa/),
# no deben trackearse acá: eran copias internas duplicadas por error.
/Travexa_*.md

# Skills de diseño de referencia — Claude Code las usa localmente en cualquier
# sesión, pero NO van a control de versiones (el repo es público). No borrar del disco.
/SKILL-*.md
